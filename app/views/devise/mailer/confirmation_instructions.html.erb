<p><%= t '.greeting' %>, <strong><%= @resource.name.capitalize %></strong>!</p>

<p><%= t '.body_confirmation_instructions'  %>:</p>

<p><%= link_to t('.confirm_my_account'), confirmation_url(@resource, confirmation_token: @token) %></p>
